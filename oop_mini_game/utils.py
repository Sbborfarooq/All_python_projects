import setting # type: ignore

#defining the function to calculate the percentage
def height_percent(percentage):
    return (setting.HEIGHT/100)*percentage

def width_percent(percentage):
    return (setting.WIDTH/100)*percentage

