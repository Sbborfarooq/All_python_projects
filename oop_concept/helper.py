#when to use static_method and class_method :

"""we use static method when we want to use the  method in the class with no uniqe relationship with 
 instances(object)  like we define the is_integer method inside a class using the static mehtod its only purpose was
 to find out the num is integer or not """


"""we use the class method when we want to instantiated some instance(object) from some structured data like we 
did it in main.py in the foam of csv we create a method and instiate our objects from there """

#The main difference between them is that class_method pass first argument as object_reference
#and the static method do not pass object reference  as first argument :